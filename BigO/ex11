int factorial(int n) {
    if (n < 0) {
        return -1;
    }
    else if (n == 0) {
        return 1;
    }
    else {
        n * factorial(n - 1);
    }
}

Q: What is the time complexity?
A: Recursion from n to n - 1 to n - 2 down to 1. Such that runtime is O(N)