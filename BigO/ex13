int fib(int n) {
    if (n <= 0) return 0;
    else if (n == 1) return 1;
    return fib(n - 1) + fib(n - 2);
}

Q: What is the runtime?
A: There are two branches per call and we go as deep as N. Such that the runtime is O(2^N)