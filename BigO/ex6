void reverse(int [] array) {
    for (int i = 0; i < array.length / 2; i++) {
        int other = array.length - i - 1;
        int temp = array[i];
        array[i] = array[other];
        array[other] = temp;
    }
}

Q: What is the runtime of this code?
A: The fact we go thru only half of the array for swap however the runtime is still O(N)