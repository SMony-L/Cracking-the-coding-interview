# Compute the product of a and b
int product(int a, int b){
    int sum = 0;
    for (int i = 0; i < b; i++) {
        sum += a;
    }
    return sum;
}

Q: What is its runtime?
A: O(B) because the for loop iterate thru B