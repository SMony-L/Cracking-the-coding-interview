# Int division
int div (int a, int b) {
    int count = 0;
    int sum = b;
    while (sum <= a) {
        sum += b;
        count++;
    }
    return count;
}

Q: What is its runtime?
A: O(a/b). The while loop iterate count time which is a / b times. 